// Adicionar um ou mais elementos no final do Array e retornar o tamanho do novo array
//PUSH
const frutas = ['banana', 'maça'];
frutas.push('laranja');
console.log(frutas);

const arr = ['banana', 'maça', 'laranja'];
const arrLength = arr.push('acerola');
console.log(arrLength);

console.log(arr);